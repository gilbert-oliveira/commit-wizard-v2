name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  release:
    types: [published]

env:
  NODE_VERSION: '18'
  BUN_VERSION: '1.2.13'

jobs:
  # Testes e Qualidade de Código
  test:
    name: Testes e Qualidade
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Cache dependências
        uses: actions/cache@v3
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Instalar dependências
        run: bun install

      - name: Executar testes
        run: bun test

      - name: Executar testes com cobertura
        run: bun run test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
          verbose: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

      - name: Build do projeto
        run: bun run build

      - name: Verificar build
        run: |
          if [ -f "dist/commit-wizard.js" ]; then
            echo "✅ Build gerado com sucesso"
            ls -la dist/
          else
            echo "❌ Build falhou"
            exit 1
          fi

  # Linting e Formatação
  lint:
    name: Linting e Formatação
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Instalar dependências
        run: bun install

      - name: Verificar estrutura do código
        run: |
          echo "🔍 Verificando estrutura do código..."
          echo "📁 Arquivos TypeScript:"
          find src/ -name "*.ts" | wc -l
          echo "📁 Arquivos de teste:"
          find . -name "*.test.ts" | wc -l
          echo "✅ Estrutura do código verificada"

      - name: Verificar tipos TypeScript
        run: bun run tsc --noEmit

  # Testes de Integração
  integration:
    name: Testes de Integração
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Instalar dependências
        run: bun install

      - name: Configurar Git para testes
        run: |
          git config --global user.name "CI Bot"
          git config --global user.email "ci@example.com"

      - name: Executar testes de integração
        run: bun test tests/integration.test.ts

      - name: Executar testes Smart Split
        run: bun test tests/smart-split.test.ts

  # Build e Teste de Instalação
  build-test:
    name: Build e Teste de Instalação
    runs-on: ubuntu-latest
    needs: [test, lint]

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Instalar dependências
        run: bun install

      - name: Build do projeto
        run: bun run build

      - name: Testar instalação local
        run: |
          # Simular instalação local
          bun run build
          chmod +x dist/commit-wizard.js

          # Testar execução básica
          echo "Testando execução do CLI..."
          timeout 10s bun run dist/commit-wizard.js --help || true

      - name: Verificar arquivos de distribuição
        run: |
          echo "📦 Arquivos de distribuição:"
          ls -la dist/
          echo ""
          echo "📄 Conteúdo do package.json:"
          cat package.json | jq '.files'

  # Publicação no NPM (apenas em releases)
  publish:
    name: Publicar no NPM
    runs-on: ubuntu-latest
    needs: [test, lint, integration, build-test]
    if: github.event_name == 'release' && github.event.action == 'published'

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Instalar dependências
        run: bun install

      - name: Build do projeto
        run: bun run build

      - name: Configurar NPM
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
          npm config set access public

      - name: Publicar no NPM
        run: npm publish
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  # Criar Release no GitHub
  release:
    name: Criar Release
    runs-on: ubuntu-latest
    needs: [test, lint, integration, build-test]
    if: github.event_name == 'release' && github.event.action == 'published'

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Build do projeto
        run: bun run build

      - name: Criar arquivo de distribuição
        run: |
          mkdir -p release
          cp -r dist/ release/
          cp package.json README.md LICENSE .commit-wizardrc release/
          cd release && tar -czf commit-wizard-${{ github.event.release.tag_name }}.tar.gz *

      - name: Upload assets para release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./release/commit-wizard-${{ github.event.release.tag_name }}.tar.gz
          asset_name: commit-wizard-${{ github.event.release.tag_name }}.tar.gz
          asset_content_type: application/gzip
